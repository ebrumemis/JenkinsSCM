pipeline {
    agent any

    stages {
        stage('Timeout Header') {
            steps {
                timeout(time:3,unit:'SECONDS') {
                echo 'Sleep'
                }
            }
        }
    }
}